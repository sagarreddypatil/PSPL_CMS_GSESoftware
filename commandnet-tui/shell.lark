?start: cmd
?cmd: "exec" WORD -> exec
    | "all" "cmds" -> all_cmds
    | "set" WORD eval -> set_var
    | "get" WORD -> get_var
    | "all" "vars" -> all_vars
eval: NUMBER
    | "`" [^\`]* "`" -> eval

%import common.WORD
%import common.NUMBER
%import common.WS_INLINE
%ignore WS_INLINE
